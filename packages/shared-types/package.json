{
  "name": "@medgram/shared-types",
  "version": "0.1.0",
  "private": true,
  "description": "Tipos compartidos y reglas de compliance como código (fuente de verdad: docs/COMPLIANCE.md)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -w -p tsconfig.json",
    "test": "npm run build && node --test test/compliance.test.mjs test/topics.test.mjs"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
